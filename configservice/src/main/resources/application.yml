server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yohanko1/msa-config
          default-label: main
          search-paths: '{application}'
